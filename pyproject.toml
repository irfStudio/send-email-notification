[build-system]
requires = ["setuptools>=42"]
build-backend = "setuptools.build_meta"

[tool.ruff]
line-length = 130

[tool.black]
line-length = 130

[tool.isort]
profile = "black"

[tool.coverage.report]
include = ["sendemail.py"]
